repos:
  - repo: https://github.com/gitleaks/gitleaks
    # Frozen to the commit behind the tag (`pre-commit autoupdate --freeze`
    # style): a tag can be moved, a commit SHA cannot. Keep in step with
    # GITLEAKS_VERSION in .github/workflows/gitleaks.yml.
    rev: 83d9cd684c87d95d656c1458ef04895a7f1cbd8e  # frozen: v8.30.1
    hooks:
      - id: gitleaks
